{
  "name": "grosint-website",
  "version": "1.0.0",
  "description": "Grosint — Sovereign Intelligence Platform — Marketing Website",
  "scripts": {
    "dev": "npx concurrently \"npx postcss css/style.css -o dist/style.css --watch\" \"npx serve .\"",
    "build": "NODE_ENV=production npx postcss css/style.css -o dist/style.css",
    "watch": "npx postcss css/style.css -o dist/style.css --watch"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.0",
    "cssnano": "^8.0.1",
    "postcss": "^8.4.0",
    "postcss-cli": "^11.0.1",
    "postcss-import": "^16.1.1",
    "tailwindcss": "^3.4.0"
  }
}
